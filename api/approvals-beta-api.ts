/* tslint:disable */
/* eslint-disable */
/**
 * Fireblocks API
 * Fireblocks provides a suite of applications to manage digital asset operations and a complete development platform to build your business on the blockchain.  - Visit our website for more information: [Fireblocks Website](https://fireblocks.com) - Visit our developer docs: [Fireblocks DevPortal](https://developers.fireblocks.com) 
 *
 * The version of the OpenAPI document: 1.6.2
 * Contact: developers@fireblocks.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
import { convertToFireblocksResponse } from "../response/fireblocksResponse";
// URLSearchParams not necessarily used
// @ts-ignore
import { URL, URLSearchParams } from 'url';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
import { assertParamExistsAndNotEmpty } from '../utils/validation_utils';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import { ApprovalRequestItem } from '../models';
// @ts-ignore
import { ErrorSchema } from '../models';
// @ts-ignore
import { ListApprovalsResponse } from '../models';
/**
 * ApprovalsBetaApi - axios parameter creator
 * @export
 */
export const ApprovalsBetaApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Retrieve full detail for a single approval request by ID, including the payload to sign and, when requested, the request\'s `quorumStatus`.  Because this endpoint addresses one request, it accepts `quorumStatusMode=FULL`, which adds the participating approvers and their individual approval state.  `userStatus` reflects the authenticated user by default. Pass `userId` to report it for another user instead.  Endpoint Permission: Owner, Admin, Non-Signing Admin, Approver, Signer, Security Admin, Security Auditor.
         * @summary Get a single approval request
         * @param {string} requestId The approval request ID.
         * @param {string} [userId] Report &#x60;userStatus&#x60; for this user instead of the authenticated user. This selects whose approval state is returned; it does not change which requests can be fetched. Requires an Admin, Non-Signing Admin, Security Admin or Security Auditor role; other roles are rejected with 403.
         * @param {GetApprovalByIdQuorumStatusModeEnum} [quorumStatusMode] How much quorum detail to include in &#x60;quorumStatus&#x60;. &#x60;NONE&#x60; (the default) returns it as &#x60;null&#x60;. &#x60;SUMMARY&#x60; returns the approval thresholds, current counts and status. &#x60;FULL&#x60; adds &#x60;users&#x60; and the per-group &#x60;members&#x60; indexes identifying who may approve and who already has. Any other value is rejected with 400; the parameter is case-sensitive.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getApprovalById: async (requestId: string, userId?: string, quorumStatusMode?: GetApprovalByIdQuorumStatusModeEnum, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            assertParamExistsAndNotEmpty('getApprovalById', 'requestId', requestId)
            const localVarPath = `/approvals/{requestId}`
                .replace(`{${"requestId"}}`, encodeURIComponent(String(requestId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (userId !== undefined) {
                localVarQueryParameter['userId'] = userId;
            }

            if (quorumStatusMode !== undefined) {
                localVarQueryParameter['quorumStatusMode'] = quorumStatusMode;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Retrieve the pending approval requests the authenticated API user is eligible to act on, including requests the user has already approved that are still pending overall.  The response is scoped to the authenticated user by default. Pass `userId` to read another user\'s queue, or `includeAllUsers=true` to read every pending request in the workspace. Both require an Admin, Non-Signing Admin, Security Admin or Security Auditor role and are rejected with 403 otherwise.  Set `quorumStatusMode=SUMMARY` to include each request\'s approval thresholds and counts. The per-approver breakdown is available only when fetching a single request — see `GET /approvals/{requestId}`.  Endpoint Permission: Owner, Admin, Non-Signing Admin, Approver, Signer, Security Admin, Security Auditor.
         * @summary List approval requests
         * @param {boolean} [includeUserApproved] When true, also include requests the authenticated user has already approved that are still pending overall. Defaults to false (only requests the user has not yet acted on).
         * @param {string} [userId] Return the pending requests for this user instead of the authenticated user. Requires an Admin, Non-Signing Admin, Security Admin or Security Auditor role; other roles are rejected with 403. Cannot be combined with &#x60;includeAllUsers&#x3D;true&#x60; — sending both is rejected with 400.
         * @param {boolean} [includeAllUsers] When true, return every pending request in the workspace instead of a single user\&#39;s queue. Defaults to false. Requires an Admin, Non-Signing Admin, Security Admin or Security Auditor role; other roles are rejected with 403. In this mode &#x60;userStatus&#x60; is always &#x60;USER_STATUS_NOT_APPLICABLE&#x60;, because the response is not scoped to one user, and &#x60;includeUserApproved&#x60; has no effect. Cannot be combined with &#x60;userId&#x60;.
         * @param {GetApprovalsQuorumStatusModeEnum} [quorumStatusMode] How much quorum detail to include in each request\&#39;s &#x60;quorumStatus&#x60;. &#x60;NONE&#x60; (the default) returns it as &#x60;null&#x60;. &#x60;SUMMARY&#x60; returns the approval thresholds, current counts and status. &#x60;FULL&#x60; is rejected with 400 on this endpoint because the per-approver breakdown requires a single request — use &#x60;GET /approvals/{requestId}&#x60; for it. Any other value is rejected with 400; the parameter is case-sensitive.
         * @param {number} [pageSize] Number of results per page. Maximum 30. Defaults to 20.
         * @param {string} [pageCursor] Cursor returned from the previous response (the &#x60;next&#x60; field) to fetch the next page.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getApprovals: async (includeUserApproved?: boolean, userId?: string, includeAllUsers?: boolean, quorumStatusMode?: GetApprovalsQuorumStatusModeEnum, pageSize?: number, pageCursor?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/approvals`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (includeUserApproved !== undefined) {
                localVarQueryParameter['includeUserApproved'] = includeUserApproved;
            }

            if (userId !== undefined) {
                localVarQueryParameter['userId'] = userId;
            }

            if (includeAllUsers !== undefined) {
                localVarQueryParameter['includeAllUsers'] = includeAllUsers;
            }

            if (quorumStatusMode !== undefined) {
                localVarQueryParameter['quorumStatusMode'] = quorumStatusMode;
            }

            if (pageSize !== undefined) {
                localVarQueryParameter['pageSize'] = pageSize;
            }

            if (pageCursor !== undefined) {
                localVarQueryParameter['pageCursor'] = pageCursor;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * ApprovalsBetaApi - functional programming interface
 * @export
 */
export const ApprovalsBetaApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = ApprovalsBetaApiAxiosParamCreator(configuration)
    return {
        /**
         * Retrieve full detail for a single approval request by ID, including the payload to sign and, when requested, the request\'s `quorumStatus`.  Because this endpoint addresses one request, it accepts `quorumStatusMode=FULL`, which adds the participating approvers and their individual approval state.  `userStatus` reflects the authenticated user by default. Pass `userId` to report it for another user instead.  Endpoint Permission: Owner, Admin, Non-Signing Admin, Approver, Signer, Security Admin, Security Auditor.
         * @summary Get a single approval request
         * @param {string} requestId The approval request ID.
         * @param {string} [userId] Report &#x60;userStatus&#x60; for this user instead of the authenticated user. This selects whose approval state is returned; it does not change which requests can be fetched. Requires an Admin, Non-Signing Admin, Security Admin or Security Auditor role; other roles are rejected with 403.
         * @param {GetApprovalByIdQuorumStatusModeEnum} [quorumStatusMode] How much quorum detail to include in &#x60;quorumStatus&#x60;. &#x60;NONE&#x60; (the default) returns it as &#x60;null&#x60;. &#x60;SUMMARY&#x60; returns the approval thresholds, current counts and status. &#x60;FULL&#x60; adds &#x60;users&#x60; and the per-group &#x60;members&#x60; indexes identifying who may approve and who already has. Any other value is rejected with 400; the parameter is case-sensitive.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getApprovalById(requestId: string, userId?: string, quorumStatusMode?: GetApprovalByIdQuorumStatusModeEnum, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ApprovalRequestItem>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getApprovalById(requestId, userId, quorumStatusMode, options);
            const index = configuration?.serverIndex ?? 0;
            const operationBasePath = operationServerMap['ApprovalsBetaApi.getApprovalById']?.[index]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, operationBasePath || basePath);
        },
        /**
         * Retrieve the pending approval requests the authenticated API user is eligible to act on, including requests the user has already approved that are still pending overall.  The response is scoped to the authenticated user by default. Pass `userId` to read another user\'s queue, or `includeAllUsers=true` to read every pending request in the workspace. Both require an Admin, Non-Signing Admin, Security Admin or Security Auditor role and are rejected with 403 otherwise.  Set `quorumStatusMode=SUMMARY` to include each request\'s approval thresholds and counts. The per-approver breakdown is available only when fetching a single request — see `GET /approvals/{requestId}`.  Endpoint Permission: Owner, Admin, Non-Signing Admin, Approver, Signer, Security Admin, Security Auditor.
         * @summary List approval requests
         * @param {boolean} [includeUserApproved] When true, also include requests the authenticated user has already approved that are still pending overall. Defaults to false (only requests the user has not yet acted on).
         * @param {string} [userId] Return the pending requests for this user instead of the authenticated user. Requires an Admin, Non-Signing Admin, Security Admin or Security Auditor role; other roles are rejected with 403. Cannot be combined with &#x60;includeAllUsers&#x3D;true&#x60; — sending both is rejected with 400.
         * @param {boolean} [includeAllUsers] When true, return every pending request in the workspace instead of a single user\&#39;s queue. Defaults to false. Requires an Admin, Non-Signing Admin, Security Admin or Security Auditor role; other roles are rejected with 403. In this mode &#x60;userStatus&#x60; is always &#x60;USER_STATUS_NOT_APPLICABLE&#x60;, because the response is not scoped to one user, and &#x60;includeUserApproved&#x60; has no effect. Cannot be combined with &#x60;userId&#x60;.
         * @param {GetApprovalsQuorumStatusModeEnum} [quorumStatusMode] How much quorum detail to include in each request\&#39;s &#x60;quorumStatus&#x60;. &#x60;NONE&#x60; (the default) returns it as &#x60;null&#x60;. &#x60;SUMMARY&#x60; returns the approval thresholds, current counts and status. &#x60;FULL&#x60; is rejected with 400 on this endpoint because the per-approver breakdown requires a single request — use &#x60;GET /approvals/{requestId}&#x60; for it. Any other value is rejected with 400; the parameter is case-sensitive.
         * @param {number} [pageSize] Number of results per page. Maximum 30. Defaults to 20.
         * @param {string} [pageCursor] Cursor returned from the previous response (the &#x60;next&#x60; field) to fetch the next page.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getApprovals(includeUserApproved?: boolean, userId?: string, includeAllUsers?: boolean, quorumStatusMode?: GetApprovalsQuorumStatusModeEnum, pageSize?: number, pageCursor?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ListApprovalsResponse>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getApprovals(includeUserApproved, userId, includeAllUsers, quorumStatusMode, pageSize, pageCursor, options);
            const index = configuration?.serverIndex ?? 0;
            const operationBasePath = operationServerMap['ApprovalsBetaApi.getApprovals']?.[index]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, operationBasePath || basePath);
        },
    }
};

/**
 * ApprovalsBetaApi - factory interface
 * @export
 */
export const ApprovalsBetaApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = ApprovalsBetaApiFp(configuration)
    return {
        /**
         * Retrieve full detail for a single approval request by ID, including the payload to sign and, when requested, the request\'s `quorumStatus`.  Because this endpoint addresses one request, it accepts `quorumStatusMode=FULL`, which adds the participating approvers and their individual approval state.  `userStatus` reflects the authenticated user by default. Pass `userId` to report it for another user instead.  Endpoint Permission: Owner, Admin, Non-Signing Admin, Approver, Signer, Security Admin, Security Auditor.
         * @summary Get a single approval request
         * @param {ApprovalsBetaApiGetApprovalByIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getApprovalById(requestParameters: ApprovalsBetaApiGetApprovalByIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<ApprovalRequestItem> {
            return localVarFp.getApprovalById(requestParameters.requestId, requestParameters.userId, requestParameters.quorumStatusMode, options).then((request) => request(axios, basePath));
        },
        /**
         * Retrieve the pending approval requests the authenticated API user is eligible to act on, including requests the user has already approved that are still pending overall.  The response is scoped to the authenticated user by default. Pass `userId` to read another user\'s queue, or `includeAllUsers=true` to read every pending request in the workspace. Both require an Admin, Non-Signing Admin, Security Admin or Security Auditor role and are rejected with 403 otherwise.  Set `quorumStatusMode=SUMMARY` to include each request\'s approval thresholds and counts. The per-approver breakdown is available only when fetching a single request — see `GET /approvals/{requestId}`.  Endpoint Permission: Owner, Admin, Non-Signing Admin, Approver, Signer, Security Admin, Security Auditor.
         * @summary List approval requests
         * @param {ApprovalsBetaApiGetApprovalsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getApprovals(requestParameters: ApprovalsBetaApiGetApprovalsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<ListApprovalsResponse> {
            return localVarFp.getApprovals(requestParameters.includeUserApproved, requestParameters.userId, requestParameters.includeAllUsers, requestParameters.quorumStatusMode, requestParameters.pageSize, requestParameters.pageCursor, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for getApprovalById operation in ApprovalsBetaApi.
 * @export
 * @interface ApprovalsBetaApiGetApprovalByIdRequest
 */
export interface ApprovalsBetaApiGetApprovalByIdRequest {
    /**
     * The approval request ID.
     * @type {string}
     * @memberof ApprovalsBetaApiGetApprovalById
     */
    readonly requestId: string

    /**
     * Report &#x60;userStatus&#x60; for this user instead of the authenticated user. This selects whose approval state is returned; it does not change which requests can be fetched. Requires an Admin, Non-Signing Admin, Security Admin or Security Auditor role; other roles are rejected with 403.
     * @type {string}
     * @memberof ApprovalsBetaApiGetApprovalById
     */
    readonly userId?: string

    /**
     * How much quorum detail to include in &#x60;quorumStatus&#x60;. &#x60;NONE&#x60; (the default) returns it as &#x60;null&#x60;. &#x60;SUMMARY&#x60; returns the approval thresholds, current counts and status. &#x60;FULL&#x60; adds &#x60;users&#x60; and the per-group &#x60;members&#x60; indexes identifying who may approve and who already has. Any other value is rejected with 400; the parameter is case-sensitive.
     * @type {'NONE' | 'SUMMARY' | 'FULL'}
     * @memberof ApprovalsBetaApiGetApprovalById
     */
    readonly quorumStatusMode?: GetApprovalByIdQuorumStatusModeEnum
}

/**
 * Request parameters for getApprovals operation in ApprovalsBetaApi.
 * @export
 * @interface ApprovalsBetaApiGetApprovalsRequest
 */
export interface ApprovalsBetaApiGetApprovalsRequest {
    /**
     * When true, also include requests the authenticated user has already approved that are still pending overall. Defaults to false (only requests the user has not yet acted on).
     * @type {boolean}
     * @memberof ApprovalsBetaApiGetApprovals
     */
    readonly includeUserApproved?: boolean

    /**
     * Return the pending requests for this user instead of the authenticated user. Requires an Admin, Non-Signing Admin, Security Admin or Security Auditor role; other roles are rejected with 403. Cannot be combined with &#x60;includeAllUsers&#x3D;true&#x60; — sending both is rejected with 400.
     * @type {string}
     * @memberof ApprovalsBetaApiGetApprovals
     */
    readonly userId?: string

    /**
     * When true, return every pending request in the workspace instead of a single user\&#39;s queue. Defaults to false. Requires an Admin, Non-Signing Admin, Security Admin or Security Auditor role; other roles are rejected with 403. In this mode &#x60;userStatus&#x60; is always &#x60;USER_STATUS_NOT_APPLICABLE&#x60;, because the response is not scoped to one user, and &#x60;includeUserApproved&#x60; has no effect. Cannot be combined with &#x60;userId&#x60;.
     * @type {boolean}
     * @memberof ApprovalsBetaApiGetApprovals
     */
    readonly includeAllUsers?: boolean

    /**
     * How much quorum detail to include in each request\&#39;s &#x60;quorumStatus&#x60;. &#x60;NONE&#x60; (the default) returns it as &#x60;null&#x60;. &#x60;SUMMARY&#x60; returns the approval thresholds, current counts and status. &#x60;FULL&#x60; is rejected with 400 on this endpoint because the per-approver breakdown requires a single request — use &#x60;GET /approvals/{requestId}&#x60; for it. Any other value is rejected with 400; the parameter is case-sensitive.
     * @type {'NONE' | 'SUMMARY'}
     * @memberof ApprovalsBetaApiGetApprovals
     */
    readonly quorumStatusMode?: GetApprovalsQuorumStatusModeEnum

    /**
     * Number of results per page. Maximum 30. Defaults to 20.
     * @type {number}
     * @memberof ApprovalsBetaApiGetApprovals
     */
    readonly pageSize?: number

    /**
     * Cursor returned from the previous response (the &#x60;next&#x60; field) to fetch the next page.
     * @type {string}
     * @memberof ApprovalsBetaApiGetApprovals
     */
    readonly pageCursor?: string
}

/**
 * ApprovalsBetaApi - object-oriented interface
 * @export
 * @class ApprovalsBetaApi
 * @extends {BaseAPI}
 */
export class ApprovalsBetaApi extends BaseAPI {
    /**
     * Retrieve full detail for a single approval request by ID, including the payload to sign and, when requested, the request\'s `quorumStatus`.  Because this endpoint addresses one request, it accepts `quorumStatusMode=FULL`, which adds the participating approvers and their individual approval state.  `userStatus` reflects the authenticated user by default. Pass `userId` to report it for another user instead.  Endpoint Permission: Owner, Admin, Non-Signing Admin, Approver, Signer, Security Admin, Security Auditor.
     * @summary Get a single approval request
     * @param {ApprovalsBetaApiGetApprovalByIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ApprovalsBetaApi
     */
    public getApprovalById(requestParameters: ApprovalsBetaApiGetApprovalByIdRequest) {
        return ApprovalsBetaApiFp(this.configuration).getApprovalById(requestParameters.requestId, requestParameters.userId, requestParameters.quorumStatusMode).then((request) => request(this.axios, this.basePath)).then(convertToFireblocksResponse);
    }

    /**
     * Retrieve the pending approval requests the authenticated API user is eligible to act on, including requests the user has already approved that are still pending overall.  The response is scoped to the authenticated user by default. Pass `userId` to read another user\'s queue, or `includeAllUsers=true` to read every pending request in the workspace. Both require an Admin, Non-Signing Admin, Security Admin or Security Auditor role and are rejected with 403 otherwise.  Set `quorumStatusMode=SUMMARY` to include each request\'s approval thresholds and counts. The per-approver breakdown is available only when fetching a single request — see `GET /approvals/{requestId}`.  Endpoint Permission: Owner, Admin, Non-Signing Admin, Approver, Signer, Security Admin, Security Auditor.
     * @summary List approval requests
     * @param {ApprovalsBetaApiGetApprovalsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ApprovalsBetaApi
     */
    public getApprovals(requestParameters: ApprovalsBetaApiGetApprovalsRequest = {}) {
        return ApprovalsBetaApiFp(this.configuration).getApprovals(requestParameters.includeUserApproved, requestParameters.userId, requestParameters.includeAllUsers, requestParameters.quorumStatusMode, requestParameters.pageSize, requestParameters.pageCursor).then((request) => request(this.axios, this.basePath)).then(convertToFireblocksResponse);
    }
}

/**
 * @export
 */
export const GetApprovalByIdQuorumStatusModeEnum = {
    None: 'NONE',
    Summary: 'SUMMARY',
    Full: 'FULL'
} as const;
export type GetApprovalByIdQuorumStatusModeEnum = typeof GetApprovalByIdQuorumStatusModeEnum[keyof typeof GetApprovalByIdQuorumStatusModeEnum];
/**
 * @export
 */
export const GetApprovalsQuorumStatusModeEnum = {
    None: 'NONE',
    Summary: 'SUMMARY'
} as const;
export type GetApprovalsQuorumStatusModeEnum = typeof GetApprovalsQuorumStatusModeEnum[keyof typeof GetApprovalsQuorumStatusModeEnum];
